#include <iostream>
#include <vector>

using matrix = std::vector<std::vector<int>>;

std::pair<matrix, matrix> matrix_chain_order(std::vector<int>& index)
{
	size_t n = index.size();


}


int main()
{
	return 0;
}